import pandas as pd

def createDataframe(student_data: List[List[int]]) -> pd.DataFrame:
    data = pd.DataFrame()
    data["student_id"] = [student_data[i][0] for i in range(len(student_data))]
    data["age"] = [student_data[i][1] for i in range(len(student_data))]
    return data